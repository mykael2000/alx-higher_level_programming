#!/usr/bin/node
let num = process.argv[2];

if (num === undefined || isNaN(num)){
	console.log("Missing size");
} else {
	const re = Number(num);
	let i = 0;
	while (i < x) {
		console.log("X".repeat(x));
		i++;
	}
}

